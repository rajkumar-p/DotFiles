# Ghostty Configuration
Link or move the *config* file to ~/.config/ghostty  

To soft link, navigate to the ghossty config directory and then run the command `ln -s <full_loc>/DotFiles/ghostty/config config`
